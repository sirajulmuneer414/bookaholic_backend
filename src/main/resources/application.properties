server.port=8081

spring.application.name=bookaholic

# Database
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/library_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Srjmgrz@786}
spring.jpa.hibernate.ddl-auto=update

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:doi5mhxzi}
cloudinary.api-key=${CLOUDINARY_API_KEY:927681399495334}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:change_me}

# JWT Secret (Must be 256-bit / 32+ chars long)
application.security.jwt.secret-key=${JWT_SECRET_KEY:change_me_must_be_32_chars_min}
application.security.jwt.expiration=86400000

# Admin Registration Secret (The "code" mentioned in your plan)
admin.registration.secret=${ADMIN_SECRET:change_me}

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:152323379317-j08vdu4np8hkb53f7fcs8np0igcf5ecb.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:change_me}
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI:http://localhost:8081/login/oauth/code/google}
# Scopes can be comma-separated in properties files
spring.security.oauth2.client.registration.google.scope=openid,profile,email

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo

# File Upload Properties
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email Configuration for Password Reset
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:boogieman9990@gmail.com}
spring.mail.password=${MAIL_PASSWORD:change_me}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Password Reset Token Expiry (in minutes)
password.reset.token.expiry=60

# OTP Configuration
otp.expiry.minutes=10
otp.length=6